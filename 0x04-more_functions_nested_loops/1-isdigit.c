#include "main.h"
/**
 * _isupper - find if it is capitalized
 * @c: input parameter.
 * Return: 1 if is capital letter character, 0 otherwise
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
